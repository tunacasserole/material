<section id="content">
    <div class="container-fluid">
        <%= User.current_user.is_admin ? render(partial: 'dashboard/admin/header') : render(partial: 'dashboard/user/header') %>
        <%= User.current_user.is_admin ? render(partial: 'dashboard/admin/layout') : render(partial: 'dashboard/user/layout') %>
        <%= User.current_user.is_admin ? render(partial: 'dashboard/admin/footer') : render(partial: 'dashboard/user/footer') %>
    </div>
</section>
